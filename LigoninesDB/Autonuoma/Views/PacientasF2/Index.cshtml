@model IEnumerable<Org.Ktu.Isk.P175B602.Autonuoma.Models.PacientasF2.PacientasL>

@* this will also be used as page title in main layout *@
@{ ViewData["title"] = "Pacientai (F2)"; }

@* content header *@
<div class="main-content-header">
	<span class="title">@ViewData["title"]</span>
	<span class="actions">
		<a href="@Url.Action("Create")" class="btn btn-primary"><i class="fa-solid fa-plus"></i> Sukurti naują</a>
	</span>
</div>

@* content itself *@
<div class="main-content-lst">
	<table class="entity-list">
		<thead>
			<tr>
				<th>
					@Html.DisplayNameFor(m => m.Asmens_Kod)
				</th>
				<th>
					@Html.DisplayNameFor(m => m.Pavarde)
				</th>
				<th>
					@Html.DisplayNameFor(m => m.Vardas)
				</th>
				<th>
					@Html.DisplayNameFor(m => m.Sesele)
				</th>
				<th></th>
			</tr>
		</thead>
		<tbody>
			@foreach (var item in Model) {
			<tr>
				<td>
					@Html.DisplayFor(m => item.Asmens_Kod)
				</td>
				<td>
					@Html.DisplayFor(m => item.Pavarde)
				</td>
				<td>
					@Html.DisplayFor(m => item.Vardas)
				</td>
				<td>
					@Html.DisplayFor(m => item.Sesele)
				</td>
				<td>
					<a 
						href="@Url.Action("Edit", new { id = item.Asmens_Kod })" 
						class="btn btn-primary"
						><i class="fa-solid fa-pen-to-square"></i> Redaguoti</a>
						
					<a 
						href="@Url.Action("Delete", new { id = item.Asmens_Kod })" 
						class="btn btn-danger"
						><i class="fa-solid fa-trash"></i> Šalinti</a>					
				</td>
			</tr>
			}
		</tbody>
	</table>
</div>